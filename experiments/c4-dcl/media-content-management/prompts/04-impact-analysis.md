# Impact Analysis

Assume one container becomes unavailable.

Using both the C4 and DCL models, determine:

- which capabilities become unavailable
- which capabilities become degraded
- which outcomes can no longer be achieved
- which external actors are affected
- which downstream services are affected

Explain how you correlated C4 containers with DCL capabilities without using a manual mapping file.
