services:
  website:
    build: .
    working_dir: /app/website
    volumes:
      - ..:/app
      - /app/website/node_modules
    ports:
      - "4321:4321"
    command: npm run dev -- --host 0.0.0.0
